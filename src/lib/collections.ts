// Firestore collection names, centralized so the form Server Actions and
// the vault dashboard all agree on them.
export const CONTACTS_COLLECTION = "contacts";
export const WAITLIST_COLLECTION = "waitlist";
