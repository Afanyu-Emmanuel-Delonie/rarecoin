// rare-blue color tokens
// Note: Tailwind v4 (in use here) prefers CSS-first config via @theme in globals.css.
// This file is a placeholder per the requested structure — confirm before use.
import type { Config } from "tailwindcss";

const config: Config = {};

export default config;
